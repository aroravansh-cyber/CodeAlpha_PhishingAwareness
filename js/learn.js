
// learn.js — topic hub (no dynamic behavior required beyond native links)
document.addEventListener('DOMContentLoaded', () => {
  // Keyboard users get the same hover affordance via :focus-visible in CSS; nothing else needed here.
});
